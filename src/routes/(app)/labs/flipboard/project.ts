import type { Project } from "$lib/labs";

const project: Project = {
  title: "Flipboard",
  description:
    "An experiment with displaying and transitioning between images using with a set of split-flap-style SVGs.",
  slug: "flipboard",
  image: "labs/flipboard/demo.webm",
};

export default project;
