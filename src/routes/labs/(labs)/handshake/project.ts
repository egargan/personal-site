import type { Project } from '$lib/labs';

const project: Project = {
  title: 'Handshake',
  description: 'A questionably-secure method of interactive authentication that asks the user to perform a secret handshake.',
  slug: 'handshake',
  image: 'labs/handshake/demo.webm',
}

export default project;
