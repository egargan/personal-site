import type { Lab } from "$labs/types";

const info: Lab = {
  title: "Flipboard",
  slug: "flipboard",
  indexDesc: "An experiment with displaying and transitioning between images using with a set of split-flap-style SVGs.",
  footerDesc: "animated image slideshow using SVG panels and CSS animation"
};

export default info;
