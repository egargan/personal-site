import type { Lab } from "$labs/types";

const info: Lab = {
  title: "Handshake",
  slug: "handshake",
  indexDesc:
    "A questionably-secure method of interactive authentication that asks the user to perform a secret handshake.",
  footerDesc: "a questionably useful interactive authentication method using secret handshakes as passphrases"
};

export default info;
