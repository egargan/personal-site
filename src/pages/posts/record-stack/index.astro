---
import Layout from "$layouts/Lab.astro";
import Lab from "./_Lab.svelte";
import { getEntry } from "astro:content";

const lab = await getEntry("labs", "record-stack");

if (!lab) {
  return Astro.redirect("/404");
}
---

<Layout {lab}>
  <Lab client:only />
</Layout>
